AOS.init();

new Typed("#typed-text", {
  strings: ["Jaya Pranay Guduru", "Frontend Developer", "Cloud Intern", "B.Tech Final Year"],
  typeSpeed: 60,
  backSpeed: 40,
  loop: true
});

function showProject(title) {
  alert("More details about: " + title);
}
